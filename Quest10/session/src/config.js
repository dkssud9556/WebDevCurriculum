import path from "path";

export const SESSION_SECRET = "session secret session secret session secret";

export const JSON_FOLDER_PATH = `${path.resolve()}/storage`;
export const JSON_FILES_PATH = `${JSON_FOLDER_PATH}/files.json`;
