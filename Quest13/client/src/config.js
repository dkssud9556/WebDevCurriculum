const path = require("path");

exports.CERTIFICATE_PATH = `${path.resolve()}/cert/CA/localhost/localhost.crt`;
exports.PRIVATE_KEY_PATH = `${path.resolve()}/cert/CA/localhost/localhost.decrypted.key`;
